import { ChangefavouritePipe } from './changefavourite.pipe';

describe('ChangefavouritePipe', () => {
  it('create an instance', () => {
    const pipe = new ChangefavouritePipe();
    expect(pipe).toBeTruthy();
  });
});
