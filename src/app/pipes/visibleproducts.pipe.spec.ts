import { VisibleproductsPipe } from './visibleproducts.pipe';

describe('VisibleproductsPipe', () => {
  it('create an instance', () => {
    const pipe = new VisibleproductsPipe();
    expect(pipe).toBeTruthy();
  });
});
